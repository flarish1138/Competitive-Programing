#include <bits/stdc++.h>
using namespace std;

int main()
{
    int i;
    int t;
    cin >> t;
    while (t--)
    {
        long long int x;
        cin >> x;
        long int a = 1;
        long int b = 1;

        for (i = a; i < x / 2; i++)
        {
            if ((i * i * i) < x && (b * b * b) < x)
        }
        for (int j = b; j < x / 2; j++)
        {
        }

        {
            cout << "YES" << endl;
        }
        else
        {
            cout << "NO" << endl;
        }
    }
}

//    while ((a * a * a) < x && (b * b * b) < x)
//     {
//         a++;
//         b++;
//     }
}

return 0;
}